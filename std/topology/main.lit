# Minimal order-extremum predicates used by MiniF2F statements that import
# Mathlib.Topology.Basic. This is not a general topology library.

prop IsLeast(S set, a S):
    forall x S:
        a <= x

prop IsGreatest(S set, a S):
    forall x S:
        x <= a
