prove:
    know:
        forall x nat:
            forall y nat:
                $p(x,y)
            iff:
                forall y nat:
                    $q(x,y)
            
        forall x nat, y nat:
            $p(x,y)

    forall x nat, y nat:
        $q(x,y)

prove:
    know a = 1
    a = 1