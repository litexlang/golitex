# 4.10 设计prop_prop的语法

# 有待实现：数学归纳法。 prop_prop
prove:
    know:
        forall p prop:
            p(0)
            forall n nat:
                p(n)
            then:
                math_induction(p)
        
        forall x nat, p prop:
            math_induction(p)
            then:
                p(x)

    p(1) # unknown
    math_induction(p)
    p(1) 

