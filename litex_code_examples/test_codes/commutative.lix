prove:
    $commutative(=)
    prop p(x nat, y nat):
        x = y
    forall x nat, y nat:
        $p(x,y)
        then:
            x = y
            y = x
            $p(y,x)
    $commutative(p)

prove:
    prop p(x nat, y nat):
        $q(x,y)

    know:
        $commutative(p)
        $p(1,2)
    $p(2,1)
    $commutative(p)
