prove:
    know $p(1)
    $p(1)
    know not $p(2)
    not $p(2)
    know exist $p(x)
    exist $p(x)
    know not exist $p()
    not exist $p()

prove:
    # forall exist
    know forall x A:
        exist $p(x)
    forall x A:
        exist $p(x)
    know forall y B:
        not exist $q()
    forall y B:
        not exist $q()
    
prove:
    # the following two statements are equivalent
    not exist $p()
    forall x A:
        # 这里还得添加 p 的定义域
        not $p(x)


prove:
    know $p(x)
    exist $p(x)
    
prove:
    prop p(x set):
        $dom(x)
        iff:
            $q(x)

    know not $p(x)
    not exist $p()

    know forall x A:
        not $p(x)    
    not exist $p()