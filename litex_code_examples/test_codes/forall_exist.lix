prove:
    exist_prop (n nat, m nat) cacuhy_gap(seq sequence, epsilon real, N nat):
        n > N
        m > N
        iff:
            abs(at(seq, n) - at(seq, m)) < epsilon

    exist_prop (N nat) Cauchy(seq sequence, epsilon real):
        epsilon > 0
        iff:
            not exist $cacuhy_gap(seq, epsilon, N)
            
            # # equivalent:
            # forall n nat, m nat:
            #     n > N
            #     m > N
            #     then:
            #         abs(at(seq, n) - at(seq, m)) < epsilon

    know:
        forall epsilon real:
            epsilon > 0
            then:
                have 1/epsilon $Cauchy(seq, epsilon)

    forall epsilon real:
        epsilon > 0
        then:
            have 1/epsilon $Cauchy(seq, epsilon)
            exist $Cauchy(seq, epsilon)
