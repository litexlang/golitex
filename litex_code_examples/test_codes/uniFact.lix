forall x set:
    p(x)
    then:
        ForallXInAPX(2)

forall x set:
    p(x)

forall y set:
    forall x set:
        p(x)
        then:
            ForallXInAPX(2)
    p(y)
    forall y set:
        p(y)
        then:
            ForallXInAPX(2)
    then:
        ForallXInAPX(2)

forall y set:
    dom:
        p(y)
        ForallXInAPX(2)
    then:
        ForallXInAPX(2)