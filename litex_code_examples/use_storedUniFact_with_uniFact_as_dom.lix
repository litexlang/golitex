prove:
    know:
        // (forall y: cond(y) => result(x,y)) => p(x)
        forall x B:
            forall y A:
                $cond(y)
                then:
                    $result(x,y)
            then:
                $p(x)

    when:
        forall y A:
            $cond(y)
            then:
                $result(1,y)
        then:
            $p(1)        
