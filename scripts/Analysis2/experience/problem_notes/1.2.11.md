# Corollary 1.2.11: closure decompositions

Set extensionality expects already-established universal membership facts.
Build one local `claim` for each inclusion before invoking `by extension`.
After an extension block, continuing with another template-membership claim
can lose the needed unfolding context in the chapter namespace; staging both
sets of claims first keeps the proof checkable.
