# Proposition 1.1.19

Proof idea: apply convergence at epsilon `1/2`, then use the zero/one law of
the discrete metric; the reverse direction has an eventually zero distance
tail. Both directions verify with one local `trust` each. Primary blocker:
`trust`.
