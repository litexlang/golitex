# Lemma 1.4.3

Proof idea: prove a strictly increasing positive-index map satisfies
`phi(n) >= n`, then reuse the original convergence tail. The source-facing
theorem runs with one local `trust`. Primary blocker: `trust`.
