# Theorem 2.1.4 continuity characterizations

## Task context

- Task: Continue and complete Tao Analysis II Chapter 2.
- Scope: Theorem 2.1.4 in the Chapter 2 final file and structured record.
- Related workspace: `textbooks/Analysis2/` and `scripts/Analysis2/`.

## Mathematical idea

Epsilon--delta continuity transports a convergent sequence's eventual
delta-tail to an epsilon-tail. If continuity fails, choose for each shrinking
radius a point whose image remains a fixed positive distance away. For the
neighborhood form, use metric balls in each direction.

## Current Litex shape

The three clauses are concrete predicates:
`is_metric_continuous_at`, `preserves_metric_limits_at`, and
`has_metric_open_neighborhood_form_at`. The forward sequential direction is
checked by transporting the `delta / 2` convergence tail through the selected
continuity delta. Three directional theorems retain narrow trusts.

## Remaining blocker

The source assigns the proof to Exercise 2.1.1. The remaining reverse sequential
direction requires a countable shrinking-ball selection, while the
neighborhood directions depend on the Chapter 1 pointwise-to-subset bridges
already recorded in `scripts/Analysis2/todo.md`.

Status: translated. Primary blocker label: `trust`.

Verifier command:

```text
target/release/litex -compact -f textbooks/Analysis2/chapter02-continuous-functions.lit
```

Result on 2026-07-26: success.
