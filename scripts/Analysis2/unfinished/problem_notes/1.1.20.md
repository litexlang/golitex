# Proposition 1.1.20

Proof idea: assume two distinct limits, use
`epsilon = dist(a,b)/3`, and contradict the triangle inequality. The theorem
interface verifies with one local `trust`. Primary blocker: `trust`.
