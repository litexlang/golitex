# Proposition 1.4.5

Proof idea: recursively choose increasingly late terms in `1/j` balls; in the
reverse direction take a convergent subsequence term after the maximum of the
requested lower index and the convergence-tail index. The reverse direction
is checkable. The forward direction retains its local `trust` because it needs
recursive point selection. Primary blocker: `trust`.
