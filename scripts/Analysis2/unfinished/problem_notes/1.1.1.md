# Lemma 1.1.1

Proof idea: unfold the two epsilon-tail definitions and simplify
`abs(abs(u(n)-a)-0)`. The current two Litex directions are runnable, but each
uses one `trust`. Runner result: `success`; no untrusted proof failure is being
hidden. Primary blocker: `trust`.
