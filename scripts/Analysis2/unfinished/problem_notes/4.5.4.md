# Proposition 4.5.4

Proof idea: extend the rational identity by continuity in the exponent.

Current attempt: direct `exp_real(x) = euler_number^x` ended in `ExecStmtError` because real-power well-definedness is not imported. The graph-relation formulation passes. Remaining trust: equality with the supplied real-power value. Primary blocker: `trust`.
