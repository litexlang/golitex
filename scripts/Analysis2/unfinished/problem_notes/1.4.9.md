# Lemma 1.4.9

Proof idea: compare a late original term with a sufficiently late subsequence
term, then compare that subsequence term with the limit. The theorem runs with
one local `trust`. Primary blocker: `trust`.
