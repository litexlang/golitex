# Proposition 1.4.12

Proof idea: use sequential closure for complete subspaces, and note that
Cauchy and convergence statements use the same restricted distance for the
converse. Both directions run with local `trust`. Primary blocker: `trust`.
