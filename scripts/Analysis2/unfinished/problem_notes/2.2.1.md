# Lemma 2.2.1 continuity of real function pairs

## Task context

- Task: Continue and complete Tao Analysis II Chapter 2.
- Scope: Lemma 2.2.1 and its concrete paired-function interface.
- Related workspace: `textbooks/Analysis2/` and `scripts/Analysis2/`.

The paired map is implemented as a callable function into
`\chap1::finite_real_vector<2>`. Its first and second coordinates are selected
by the checked `real_pair_coordinate` helper.

The four pointwise/global equivalence directions remain trusted because Tao
assigns the proof to Exercise 2.2.1 and the intended route depends on the
trusted finite-dimensional convergence directions in Proposition 1.1.18.

Primary blocker label: `trust`.
