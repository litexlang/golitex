# Proposition 1.2.15

Proof idea: prove parts (a)-(h) in the source order, using complements to
transport open/closed results and pointwise ball arguments for unions and
intersections. The two open/interior directions, two closed/closure
directions, the inclusion of every open subset of E in the interior of E, and
the inclusion of the closure of E in each closed superset are now checkable.
The singleton-closed direction is also checkable: for a point z distinct
from x, the ball of radius dist(z,x)/2 cannot meet {x}, contradicting
adherence. Both complement directions are checkable through
`metric_boundary_of_complement_is_boundary`; explicit ball-formula equality
bridges avoid a template-membership proof-context failure. The closed-ball
direction is checkable: a point at distance strictly larger than the radius
has an exterior ball of half the excess distance. The open-ball,
arbitrary-union, arbitrary-intersection, interior-openness, and
closure-closedness directions retain local `trust`.
Primary blocker: `trust`.

Latest direct ball attempt: the triangle estimate for nested balls is
checkable pointwise after the new checked membership bridges
`metric_ball_member_implies_distance_lt` and
`metric_distance_lt_implies_ball_member`. Turning that local pointwise fact
into the required `$subset` statement ends in an unlocated `ExecStmtError`.
This is recorded as `trust`, not a new confirmed kernel problem.

Latest arbitrary-intersection attempt: the elementary empty-intersection
calculation checks in an outermost release `try:`. Its use against the
adherence predicate of the family intersection fails in the real source file
with `by contra: failed to execute proof`, including after an explicit
set-builder bridge. This remains `trust`, not a confirmed kernel problem.
