"""
# Wrong: fn takes in a number and return a set. Which is wrong because there are infinitely many finite sequences of integers.

fn finite_sequence_of_integers(length N) set:
    forall x finite_sequence_of_integers(length):
        x $in Z

set seq1 finite_sequence_of_integers(2)

"""

prop is_finite_sequence_of_integers(seq set):
    forall x seq:
        x $in Z

fn length_of_finite_sequence_of_integers(seq set) N