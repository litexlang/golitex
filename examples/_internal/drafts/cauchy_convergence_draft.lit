prop Cauchy_condition(x seq(R), epsilon R+, n N+):
    forall i1, j N+:
        i1 > n
        j > n
        =>:
            abs(x(i1) - x(j)) < epsilon

prop Cauchy_converge(x seq(R)):
    forall epsilon R+:
        exist n N+ st {$Cauchy_condition(x, epsilon, n)}
