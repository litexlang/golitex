thm fn_eq_implies_equality_regression:
    ? forall f, g fn(n N) N:
        forall n N:
            f(n) = g(n)
        =>:
            $fn_eq(f, g)
            f = g
            power_set(f) = power_set(g)
    by def $fn_eq(f, g)
    $fn_eq(f, g)
    f = g
    power_set(f) = power_set(g)
