let a, b, c R, d R:
    a < b = c <= d

a < d

"""
claim:
    prove:
        forall a, b, c, d R:
            2 * a <= 3 * b
            1 <= a
            d = 2
            =>:
                d + a <= 5 * b
    2 * 1 <= 2 * a
    2 * 1 = d
    d <= 2 * a
    d + a <= 2*a + a = 3*a
    d + a <= 3 * a
    know a <= 1.5 * a
    know a <= 1.5 * b
    know 4.5 * b <= 5 * b
    d + a <= 2 * a + a <= 3 * b + a <= 3 * b +1.5*b = 4.5*b <= 5 * b
    know d + a <= 5 * b
"""