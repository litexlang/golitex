abstract_prop p()
abstract_prop q()

prove:
    know:
        $p() or $q()
        not $p()

    by cases:
        prove:
            $q()
        case $p():
            impossible not $p()
        case $q():
            do_nothing
