# Builtin: closed_range nonempty when start <= end
claim:
    prove:
        $is_nonempty_set(closed_range(0, 3))
    $is_nonempty_set(closed_range(0, 3))

prove:
    forall a, b Z:
        a <= b
        =>:
            $is_nonempty_set(closed_range(a, b))
