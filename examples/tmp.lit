max(1, 2) = 2
min(1, 2) = 1

forall a, b R:
    max(a, b) = max(b, a)

forall a, b R:
    min(a, b) = min(b, a)

forall a, b R:
    a <= max(a, b)
    b <= max(a, b)

forall a, b R:
    min(a, b) <= a
    min(a, b) <= b