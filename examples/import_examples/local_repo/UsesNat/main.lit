# A local module can import a std module while it is loaded.
# The std import becomes visible to the whole runtime.

import Nat

abstract_prop local_nat_fact(n)

know:
    forall p N:
        $Nat::Prime(p)
        =>:
            $local_nat_fact(p)
