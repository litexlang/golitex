fn_template self_seq(s set):
    fn (n N_pos) s

fn self_sum(a self_seq(R), n N_pos, m N_pos) R:
    n <= m
    =>:
        self_sum(a, n, m + 1) = self_sum(a, n, m) + a(m + 1)

know forall a self_seq(R), n N_pos => self_sum(a, n, n) = a(n)

claim:
    forall a self_seq(R):
        forall m N_pos: m > 1 => self_sum(a, 1, m) = a(1) + self_sum(a, 2, m)
    prove:
        self_sum(a, 1, 1) = a(1)
        =:
            self_sum(a, 1, 2)
            self_sum(a, 1, 1) + a(2)
            self_sum(a, 1, 1) + self_sum(a, 2, 2)
            a(1) + self_sum(a, 2, 2)
        self_sum(a, 1, 2) = a(1) + self_sum(a, 2, 2)
        know forall m N_pos: m >= 2, self_sum(a, 1, m) = a(1) + self_sum(a, 2, m) => self_sum(a, 1, m + 1) = a(1) + self_sum(a, 2, m + 1)
        prove_by_induction(self_sum(a, 1, m) = a(1) + self_sum(a, 2, m), m, 2)
        know forall m N_pos: m > 1 => m >=2, self_sum(a, 1, m) = a(1) + self_sum(a, 2, m)

