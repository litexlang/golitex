fn exclude(s1, s2 set) set

know prop exclude_property(s1, s2 set):
    s1 $in set
    then:
        exclude(s1, s2) := x s1:
            not x $in s2


obj s1, s2 set
obj x exclude(s1, s2)
$exclude_property(s1, s2)
not x $in s2

# 或许可以 x $in fnName() 后，自动释放 fnName 里的性质