fn seq_sum(a seq(R), n N) R:
    seq_sum(a, n+1) = seq_sum(a, n) + a(n+1)
know forall a seq(R):
    seq_sum(a, 0) = a(0)

fn seq_product(a seq(R), n N) R:
    seq_product(a, n+1) = seq_product(a, n) * a(n+1)
know forall a seq(R):
    seq_product(a, 0) = a(0)

prove:
	obj b seq(R):
        b(1) = 1
        b(0) = 2
    =:
        seq_sum(b, 0)
        b(0)
        2
    =:
        seq_sum(b, 1)
        seq_sum(b, 0) + b(1)
        2 + 1
        3