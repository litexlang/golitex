prop is_group(G set, mul fn(G)G, inv fn(G)G, zero G)

know suppose $is_group(G, mul, inv, zero):
     forall a G:
        mul(a, inv(a)) = zero
        mul(inv(a), a) = zero
        mul(a, zero) = a
        mul(zero, a) = a
