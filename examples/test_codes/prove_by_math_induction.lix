prop p(x R, n N_pos)

let x R

know:
    forall n N_pos: n >= 2, $p(x, n) => $p(x, n+1)
    $p(x, 2)

prove_by_induction($p(x, n), n, 2)

know:
    forall n N_pos: n >= 2 => $p(x,n)

forall n N_pos: n >= 2 => $p(x,n)