fn_template_template f(x, y R):
    dom:
    	x > 1
    fn (a, b R) R:
    	dom:
        	a = x
        then:
        	f(a, b) = y

fn_template_template g(x, y R):
    fn (a, b R) R:
        dom:
        	a = x
        then:
        	g(a, b) = y