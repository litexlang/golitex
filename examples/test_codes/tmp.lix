have set s := {1, 2, 3}
have set t := x R:
	x > 1

claim:
	forall x s:
		x $in t
	prove:
		forall x s:
			x > 1

claim:
	forall x s:
		x $in t
	prove_by_contradiction:
		forall x s:
			x > 1
		x $in s
		x $in t

have set fn s(x, y R) := t R:
	t > x + y

have set s = set_defined_by_replacement(A, B, P)

have a, b st $p(x, y)

have a R, b R

prove_in_each_case:
    or:
    	a = 1
        b = 2
    then:
    	c = 3
    prove:
    	a = 2
        b = 1
    prove:
    	a = 3
        b = 1

prove_over_finite_set:
    forall x s:
        x > 1
    prove:
        x > 1

prove_by_math_induction(not $p(y, n, z), 1, 1)

claim:
	@p(x, y R):
		x > y
        then:
        	x > y
    prove:
    	x > y

@p(x, y R):
	x > y
    then:
        x > y