forall x, y N:
    x + y = y + x
    then:
        x + y = y + x

fn f(x, y N) N :
    f(x, y) = x + y

f(1,2) = 1 + 2
f(1,2) = 3

prop p(x, y N, a N, b, c N):
    iff:
        x + y = a + b + c

$p(0, 0, 0, 0, 0)
