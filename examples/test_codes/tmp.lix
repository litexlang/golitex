know @exist x R st forall_positive_number_there_is_larger_number(y R):
	dom:
        y > 0
    <=>:
        x > y

forall y R:
    y > 0
    =>:
        $forall_positive_number_there_is_larger_number(y)

        