claim:
	forall a, b R:
        a = 1
        b = 2
        then:
        	a * 2 - b = 0
            3 * a - b = 1
    prove:
        =:
            0
        	1 * 2 - 2
            a * 2 - b
        =:
            1
            3 * 1 - 2
            3 * a - b