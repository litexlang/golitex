fn logBase(x, y N) N:
    x > 0
    then:
        logBase(x, x) = 1

know forall x, y, z N:
    logBase(x^y, z) = y * logBase(x, z)
    logBase(x*y, z) = logBase(x, z) + logBase(y, z)

obj x, y, z N:
    z > 0
logBase(x, z) + logBase(y, z) $in N