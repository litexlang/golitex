fn_template self_seq(s set):
    fn (n N_pos) s

fn self_sum_of_seq_from_n_to_m(a self_seq(R), n N_pos, m N_pos) R:
    n <= m
    then:
        self_sum_of_seq_from_n_to_m(a, n, m + 1) = self_sum_of_seq_from_n_to_m(a, n, m) + a(m + 1)

know forall a self_seq(R) => self_sum_of_seq_from_n_to_m(a, 1, 1) = a(1)

prop result(a self_seq(R), m N_pos): m > 1 <=> self_sum_of_seq_from_n_to_m(a, 1, m) = a(1) + self_sum_of_seq_from_n_to_m(a, 2, m)

claim:
    forall a self_seq(R):
        forall m N_pos: m > 1 => $result(a, m)
    prove:
        