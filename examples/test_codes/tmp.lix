obj x Z

# 诡异的地方1：这里的x因为和外部世界的x同名了，所以改成了随机的另外一个东西，一旦改了就不知道怎么工作了 2. 这里传入+，没有检查+的dom是否覆盖z,z，很奇怪
exist_prop self_add fn(Z, Z)Z st exist_f_always_equal_to_add():
    forall x Z, y Z:
        self_add(x, y) = x + y

exist + st $exist_f_always_equal_to_add()