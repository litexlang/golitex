know sqrt(121) = 11

claim:
    @p(x, y R):
    	x = 5
        y = 2
        =>:
            sqrt(x ^ 3 - 2 ^ y) = 11
    prove:
    	=:
            x ^ 3 - 2 ^ y
            5 ^ 3 - 2 ^ 2
            121
        #121 >= 0
        #x ^ 3 - 2 ^ y >= 0
        =:
        	sqrt(x ^ 3 - 2 ^ y)
            sqrt(121)
            11
        sqrt(x ^ 3 - 2 ^ y) = 11