set human

prove:
    prop p1(x human)
    prop p2(x human)
    prop p3(x human)

    know:
        forall x human:
            $p1(x) 
            then:
                $p2(x)

        forall x human:
            $p2(x)
            then:
                $p3(x)

    forall x human:
        $p1(x)
        then:
            $p2(x)
            $p3(x)