fn logBase(x, y N) N:
    x > 0

know forall x, y, z N:
    logBase(z, x^y) = logBase(z, x) * y
    logBase(z, x*y) = logBase(z, x) + logBase(z, y)

obj x, y, z N:
    z > 0
logBase(z, x) + logBase(z, y) $in N