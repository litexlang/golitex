        """
        asdf
        """

# define union
know:
# define union
    forall z obj, A set, B set:
        $in(z, A)
        then:
            $in(z,union(A, B) )

    forall z obj, A set, B set:

        $in(z, B)
        then:
            $in(z,union(A, B) )

"""
asdf
"""