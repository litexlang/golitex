prop is_finite_seq(s set)
fn len(seq set) N:
    dom:
        $is_finite_seq(seq)

fn at(seq set, i N) obj:
    dom:
        $is_finite_seq(seq)
        i < len(seq)

prove:
    obj seq1 set:
        seq1 $is_finite_seq
        len(seq1) = 2
        seq1 \at 0 = 1
        seq1 \at 1 = 2

