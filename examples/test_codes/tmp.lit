claim:
    forall a, b, x R:
        x^2 + 2 * a * x + b = 0
        a^2 - b >= 0
        =>:
            x = -a + sqrt(a^2 - b) or x = -a - sqrt(a^2 - b)
    prove:
        sqrt(a^2 - b) * sqrt(a^2 - b) = sqrt(a^2 - b) ^ 2 = a^2 - b
        (x + a - sqrt(a^2 - b)) * (x + a + sqrt(a^2 - b)) = x ^ 2 + 2 * a * x + a^2 - sqrt(a^2 - b) ^ 2 = x ^ 2 + 2 * a * x + a^2 - (a^2 - b) = x ^ 2 + 2 * a * x + b = 0
        $product_is_0_then_at_least_one_factor_is_0(x + a - sqrt(a^2 - b), x + a + sqrt(a^2 - b))
        
        prove_in_each_case:
            x + a + sqrt(a^2 - b) = 0 or x + a - sqrt(a^2 - b) = 0
            =>:
                x = -a + sqrt(a^2 - b) or x = -a - sqrt(a^2 - b)
            prove:
                x + a + sqrt(a^2 - b) + (-a - sqrt(a^2 - b)) = 0 + (-a - sqrt(a^2 - b))
                x = 0 + (-a - sqrt(a^2 - b))
                x = -a - sqrt(a^2 - b) 
            prove:
                x + a - sqrt(a^2 - b) + (-a + sqrt(a^2 - b)) = 0 + (-a + sqrt(a^2 - b))
                x = 0 + (-a + sqrt(a^2 - b))
                x = -a + sqrt(a^2 - b)

claim:
    forall a, b, x1, x2 R:
        x1^2 + 2 * a * x1 + b = 0
        x2^2 + 2 * a * x2 + b = 0
        a^2 - b > 0
        x1 > x2
        =>:
            x1 = -a + sqrt(a^2 - b)
            x2 = -a - sqrt(a^2 - b)
            x1 + x2 = -2 * a
            x1 * x2 = b
    prove:
        a^2 - b > 0
        sqrt(a^2 - b) > 0
        x1^2 + 2 * a * x1 + b = 0
        a^2 - b >= 0
        x1 = -a + sqrt(a^2 - b) or x1 = -a - sqrt(a^2 - b)
        x2^2 + 2 * a * x2 + b = 0
        x2 = -a + sqrt(a^2 - b) or x2 = -a - sqrt(a^2 - b)
        
        -a + sqrt(a^2 - b) - (-a - sqrt(a^2 - b)) = -a + sqrt(a^2 - b) + a + sqrt(a^2 - b) = 2 * sqrt(a^2 - b)
        2 * sqrt(a^2 - b) > 0
        -a + sqrt(a^2 - b) > -a - sqrt(a^2 - b)
        
        claim:
            not x1 = -a - sqrt(a^2 - b)
            prove_by_contradiction:
                x1 = -a - sqrt(a^2 - b)
                prove_in_each_case:
                    x2 = -a + sqrt(a^2 - b) or x2 = -a - sqrt(a^2 - b)
                    =>:
                        not x1 > x2
                    prove:
                        x2 = -a + sqrt(a^2 - b)
                        x1 = -a - sqrt(a^2 - b)
                        -a - sqrt(a^2 - b) < -a + sqrt(a^2 - b)
                        not x1 < x2
                    prove:
                        x2 = -a - sqrt(a^2 - b)
                        x1 = -a - sqrt(a^2 - b)
                        x1 = x2
                        not x1 > x2
                x1 > x2
        x1 = -a + sqrt(a^2 - b)
        
        claim:
            not x2 = -a + sqrt(a^2 - b)
            prove_by_contradiction:
                x2 = -a + sqrt(a^2 - b)
                x1 = -a + sqrt(a^2 - b)
                x1 = x2
                not x1 > x2
                x1 > x2
        x2 = -a - sqrt(a^2 - b)

        x1 + x2 = (-a + sqrt(a^2 - b)) + (-a - sqrt(a^2 - b)) = -2 * a
        x1 * x2 = (-a + sqrt(a^2 - b)) * (-a - sqrt(a^2 - b)) = a^2 - sqrt(a^2 - b) ^ 2 = a^2 - (a^2 - b) = b
"""
claim:\n    forall a, b, x R:\n        x^2 + 2 * a * x + b = 0\n        a^2 - b >= 0\n        =>:\n            x = -a + sqrt(a^2 - b) or x = -a - sqrt(a^2 - b)\n    prove:\n        sqrt(a^2 - b) * sqrt(a^2 - b) = sqrt(a^2 - b) ^ 2 = a^2 - b\n        (x + a - sqrt(a^2 - b)) * (x + a + sqrt(a^2 - b)) = x ^ 2 + 2 * a * x + a^2 - sqrt(a^2 - b) ^ 2 = x ^ 2 + 2 * a * x + a^2 - (a^2 - b) = x ^ 2 + 2 * a * x + b = 0\n        $product_is_0_then_at_least_one_factor_is_0(x + a - sqrt(a^2 - b), x + a + sqrt(a^2 - b))\n        \n        prove_in_each_case:\n            x + a + sqrt(a^2 - b) = 0 or x + a - sqrt(a^2 - b) = 0\n            =>:\n                x = -a + sqrt(a^2 - b) or x = -a - sqrt(a^2 - b)\n            prove:\n                x + a + sqrt(a^2 - b) + (-a - sqrt(a^2 - b)) = 0 + (-a - sqrt(a^2 - b))\n                x = 0 + (-a - sqrt(a^2 - b))\n                x = -a - sqrt(a^2 - b) \n            prove:\n                x + a - sqrt(a^2 - b) + (-a + sqrt(a^2 - b)) = 0 + (-a + sqrt(a^2 - b))\n                x = 0 + (-a + sqrt(a^2 - b))\n                x = -a + sqrt(a^2 - b)\n\nclaim:\n    forall a, b, x1, x2 R:\n        x1^2 + 2 * a * x1 + b = 0\n        x2^2 + 2 * a * x2 + b = 0\n        a^2 - b > 0\n        x1 > x2\n        =>:\n            x1 = -a + sqrt(a^2 - b)\n            x2 = -a - sqrt(a^2 - b)\n            x1 + x2 = -2 * a\n            x1 * x2 = b\n    prove:\n        a^2 - b > 0\n        sqrt(a^2 - b) > 0\n        x1^2 + 2 * a * x1 + b = 0\n        a^2 - b >= 0\n        x1 = -a + sqrt(a^2 - b) or x1 = -a - sqrt(a^2 - b)\n        x2^2 + 2 * a * x2 + b = 0\n        x2 = -a + sqrt(a^2 - b) or x2 = -a - sqrt(a^2 - b)\n        \n        -a + sqrt(a^2 - b) - (-a - sqrt(a^2 - b)) = -a + sqrt(a^2 - b) + a + sqrt(a^2 - b) = 2 * sqrt(a^2 - b)\n        2 * sqrt(a^2 - b) > 0\n        -a + sqrt(a^2 - b) > -a - sqrt(a^2 - b)\n        \n        claim:\n            not x1 = -a - sqrt(a^2 - b)\n            prove_by_contradiction:\n                x1 = -a - sqrt(a^2 - b)\n                prove_in_each_case:\n                    x2 = -a + sqrt(a^2 - b) or x2 = -a - sqrt(a^2 - b)\n                    =>:\n                        not x1 > x2\n                    prove:\n                        x2 = -a + sqrt(a^2 - b)\n                        x1 = -a - sqrt(a^2 - b)\n                        -a - sqrt(a^2 - b) < -a + sqrt(a^2 - b)\n                        not x1 < x2\n                    prove:\n                        x2 = -a - sqrt(a^2 - b)\n                        x1 = -a - sqrt(a^2 - b)\n                        x1 = x2\n                        not x1 > x2\n                x1 > x2\n        x1 = -a + sqrt(a^2 - b)\n        \n        claim:\n            not x2 = -a + sqrt(a^2 - b)\n            prove_by_contradiction:\n                x2 = -a + sqrt(a^2 - b)\n                x1 = -a + sqrt(a^2 - b)\n                x1 = x2\n                not x1 > x2\n                x1 > x2\n        x2 = -a - sqrt(a^2 - b)\n\n        x1 + x2 = (-a + sqrt(a^2 - b)) + (-a - sqrt(a^2 - b)) = -2 * a\n        x1 * x2 = (-a + sqrt(a^2 - b)) * (-a - sqrt(a^2 - b)) = a^2 - sqrt(a^2 - b) ^ 2 = a^2 - (a^2 - b) = b\n
"""
