prop p(x R)
know:
    forall x R:
        x > 0
        then:
            $p(x)

claim:
    $p(1)
    prove:
        1 > 0
        $p(1)

know prop larger_is_transitive(x R, y R, z R):
    x > y
    y > z
    iff:
        x > z

claim:
    forall x R:
        x > 1
        then:
            $p(x)
    prove:
        $larger_is_transitive(x, 1, 0)
        x > 0
        $p(x)

know forall x N, y N:
    y > 0
    then:
        x + y > x

exist_prop y N st exist_N_larger_than_x(x N):
    y > x

claim:
    forall x N:
        $exist_N_larger_than_x(x)
    prove:
        x + 1 > x
        exist x+1 st $exist_N_larger_than_x(x)