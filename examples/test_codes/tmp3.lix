know forall x, y set:
    x = y
    iff:
        forall a x:
            a $in y
        forall a y:
            a $in x

fn f(s set) set
obj a set

know:
	forall x a:
		x $in f(a)
	forall x f(a):
		x $in a

f(a) = a
