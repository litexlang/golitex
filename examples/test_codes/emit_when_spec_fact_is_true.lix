prove:
    know:
        forall x nat:
            $p(x)

        forall x nat:
            $p(x)
            then:
                $q(x)

    claim propName:
        forall x nat:
            $q(x)

        prove:
            $p(x)

    $propName(2)
    $q(2)
                    
prove:
    axiom propName:
        forall x nat:
            $p(x)
            then:
                $q(x)

    know:
        $p(2)

    $propName(2)
    $q(2)
    $p(2)

prove:
    axiom propName:
        forall x nat:
            $p(x)
            then:
                $q(x)

    know $propName(2)

    $q(2)
    $p(2)