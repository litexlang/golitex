# Chapter 11 integration citations.

thm stieltjes_piecewise_integrable:
    ? forall a, b R, alpha, f fn(x '[a, b]) R:
        $is_riemann_stieltjes_integrable_on('[a, b], alpha, f)
    trust $is_riemann_stieltjes_integrable_on('[a, b], alpha, f)

thm piecewise_constant_on_interval:
    ? forall a, b R, h fn(x '[a, b]) R:
        $is_piecewise_constant_on('[a, b], h)
    trust $is_piecewise_constant_on('[a, b], h)
